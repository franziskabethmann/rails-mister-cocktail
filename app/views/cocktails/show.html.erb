<div class="container">
  <h1> <%= @cocktail.name %> </h1>
   <%= cl_image_tag @cocktail.photo,
      width: 600, height: 300, crop: :fill %>
  <br>
  <br>

  <div class="ingredients">
    <h4>Ingredients:</h4>
    <ul style= "list-style-type: none">
      <% @cocktail.doses.each do |dose| %>
        <li><%= dose.ingredient.name %>: <%=dose.description %>
          <%= link_to "✗", dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } %>
        </li>
      <% end %>
    </ul>
  </div>


<p><%= link_to "+ Add Dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary" %></p>
<p><%= link_to "Back to Cocktail Overview", cocktails_path %></p>

</div>

